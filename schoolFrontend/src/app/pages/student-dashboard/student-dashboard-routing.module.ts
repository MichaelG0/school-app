import { Routes } from '@angular/router';
import { StudentDashboardComponent } from './student-dashboard.component';

export const routes: Routes = [{ path: '', component: StudentDashboardComponent }];
