import { Routes } from '@angular/router';
import { TeacherDashboardComponent } from './teacher-dashboard.component';

export const routes: Routes = [{ path: '', component: TeacherDashboardComponent }];
